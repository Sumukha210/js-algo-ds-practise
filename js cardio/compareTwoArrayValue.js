// write a function name "same" which accepts two array
// and the value in second array must be sqaure of the first array
//frequency of values must be same

const same = (arr1, arr2) => {
	//compare two arr values
	//return true,if values in first arr2 is square of the arr1,else return false
};

console.log(same([1, 2, 3], [1, 4, 9]));
